"""Provider interfaces for every external integration.

Planned: TelephonyProvider (Plivo, later Exotel), STTProvider, TTSProvider,
LLMProvider (live + post-call), WhatsAppProvider, StorageProvider.
"""
